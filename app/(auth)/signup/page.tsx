import Link from "next/link";
import { AuthForm } from "@/components/auth/auth-form";
export default function SignupPage() { return <section className="w-full max-w-sm"><p className="text-sm font-semibold text-primary">TOEIC 900+ Error Log</p><h1 className="mt-2 text-3xl font-semibold">Start your error loop</h1><p className="mb-8 mt-2 text-sm text-muted-foreground">Capture less. Diagnose deeper. Stop repeating the same mistake.</p><AuthForm mode="signup" /><p className="mt-6 text-sm text-muted-foreground">Already registered? <Link className="font-medium text-primary hover:underline" href="/login">Log in</Link></p></section>; }
